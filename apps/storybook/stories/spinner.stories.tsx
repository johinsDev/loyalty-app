import { Spinner } from "@loyalty/ui";

const meta = { title: "Components/Spinner", component: Spinner, tags: ["autodocs"] };
export default meta;

export const Default = { render: () => <Spinner /> };
