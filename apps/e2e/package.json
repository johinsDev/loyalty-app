{
  "name": "@loyalty/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "lint": "oxlint tests playwright.config.ts",
    "format": "oxlint tests playwright.config.ts --fix",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo node_modules test-results playwright-report"
  },
  "devDependencies": {
    "@loyalty/tsconfig": "workspace:*",
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.0.0",
    "oxlint": "^0.15.0",
    "typescript": "^5.7.0"
  }
}
