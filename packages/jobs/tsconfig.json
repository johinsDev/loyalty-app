{
  "extends": "@loyalty/tsconfig/base.json",
  // `react-jsx` so tsc can type-check the `@loyalty/email-templates` (.tsx)
  // source it pulls in for the magic-link email render helper.
  "compilerOptions": { "jsx": "react-jsx" },
  "include": ["trigger", "log.ts", "trigger.config.ts"],
  "exclude": ["node_modules", "dist", ".trigger"]
}
